# 数据库设计文档

## 一、数据库概述

### 1.1 数据库信息

| 项目 | 说明 |
|------|------|
| 数据库类型 | MySQL 8.0 |
| 数据库名称 | career_backend |
| 字符集 | utf8mb4 |
| 排序规则 | utf8mb4_unicode_ci |
| 存储引擎 | InnoDB |

### 1.2 数据库结构概览

```
career_backend/
├── 用户相关表
│   ├── user                    # 用户表
│   ├── user_profile            # 用户档案表
│   └── feedback                # 用户反馈表
│
├── 简历相关表
│   ├── resume                  # 简历表
│   ├── resume_education        # 教育经历表
│   ├── resume_work_experience  # 工作经历表
│   ├── resume_project          # 项目经历表
│   ├── resume_skill            # 技能表
│   └── resume_certificate      # 证书表
│
├── 面试相关表
│   ├── interview               # 面试记录表
│   ├── interview_question      # 面试题目表
│   └── interview_review        # 面试回顾表
│
├── 职位相关表
│   ├── job                     # 职位表
│   ├── job_skill               # 职位技能要求表
│   └── user_job_match          # 用户-职位匹配表
│
├── 会员相关表
│   ├── member_package          # 会员套餐表
│   ├── member_subscription     # 会员订阅表
│   ├── payment_order           # 支付订单表
│   └── points_transaction      # 积分交易表
│
├── 知识库相关表
│   ├── career_path             # 职业发展路径表
│   ├── skill_tree              # 技能树表
│   └── industry_knowledge      # 行业知识表
│
└── 系统相关表
    ├── file_upload             # 文件上传记录表
    ├── system_config           # 系统配置表
    └── operation_log           # 操作日志表
```

## 二、核心表设计

### 2.1 用户表（user）

```sql
CREATE TABLE `user` (
    `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '用户ID',
    `username` VARCHAR(50) NOT NULL COMMENT '用户名',
    `password` VARCHAR(255) NOT NULL COMMENT '密码（加密存储）',
    `email` VARCHAR(100) COMMENT '邮箱',
    `phone` VARCHAR(20) COMMENT '手机号',
    `avatar` VARCHAR(500) COMMENT '头像URL',
    `status` TINYINT DEFAULT 1 COMMENT '状态：0-禁用，1-正常',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `last_login_time` DATETIME COMMENT '最后登录时间',
    `last_login_ip` VARCHAR(50) COMMENT '最后登录IP',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_username` (`username`),
    UNIQUE KEY `uk_email` (`email`),
    UNIQUE KEY `uk_phone` (`phone`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

### 2.2 用户档案表（user_profile）

```sql
CREATE TABLE `user_profile` (
    `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '档案ID',
    `user_id` BIGINT NOT NULL COMMENT '用户ID',
    `real_name` VARCHAR(50) COMMENT '真实姓名',
    `gender` TINYINT COMMENT '性别：0-未知，1-男，2-女',
    `birth_date` DATE COMMENT '出生日期',
    `city` VARCHAR(50) COMMENT '所在城市',
    `university` VARCHAR(100) COMMENT '毕业/就读院校',
    `major` VARCHAR(100) COMMENT '专业',
    `education` TINYINT COMMENT '学历：1-大专，2-本科，3-硕士，4-博士',
    `graduation_year` INT COMMENT '毕业年份',
    `work_years` INT COMMENT '工作年限',
    `current_position` VARCHAR(100) COMMENT '当前职位',
    `current_company` VARCHAR(100) COMMENT '当前公司',
    `desired_position` VARCHAR(100) COMMENT '期望职位',
    `desired_city` VARCHAR(50) COMMENT '期望城市',
    `expected_salary` VARCHAR(50) COMMENT '期望薪资',
    `self_introduction` TEXT COMMENT '自我介绍',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户档案表';
```

### 2.3 简历表（resume）

```sql
CREATE TABLE `resume` (
    `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '简历ID',
    `user_id` BIGINT NOT NULL COMMENT '用户ID',
    `title` VARCHAR(100) NOT NULL COMMENT '简历标题',
    `template_id` INT DEFAULT 1 COMMENT '模板ID',
    `content` JSON COMMENT '简历内容（JSON格式存储）',
    `is_default` TINYINT DEFAULT 0 COMMENT '是否默认简历：0-否，1-是',
    `completeness` INT DEFAULT 0 COMMENT '完整度百分比',
    `view_count` INT DEFAULT 0 COMMENT '浏览次数',
    `download_count` INT DEFAULT 0 COMMENT '下载次数',
    `status` TINYINT DEFAULT 1 COMMENT '状态：0-草稿，1-公开，2-保密',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`),
    KEY `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='简历表';
```

### 2.4 教育经历表（resume_education）

```sql
CREATE TABLE `resume_education` (
    `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `resume_id` BIGINT NOT NULL COMMENT '简历ID',
    `school` VARCHAR(100) NOT NULL COMMENT '学校名称',
    `degree` TINYINT NOT NULL COMMENT '学历：1-大专，2-本科，3-硕士，4-博士',
    `major` VARCHAR(100) COMMENT '专业',
    `start_date` DATE NOT NULL COMMENT '开始时间',
    `end_date` DATE COMMENT '结束时间',
    `is_current` TINYINT DEFAULT 0 COMMENT '是否在校：0-否，1-是',
    `gpa` DECIMAL(3,2) COMMENT 'GPA',
    `description` TEXT COMMENT '在校经历描述',
    `sort_order` INT DEFAULT 0 COMMENT '排序序号',
    PRIMARY KEY (`id`),
    KEY `idx_resume_id` (`resume_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='教育经历表';
```

### 2.5 工作经历表（resume_work_experience）

```sql
CREATE TABLE `resume_work_experience` (
    `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `resume_id` BIGINT NOT NULL COMMENT '简历ID',
    `company` VARCHAR(100) NOT NULL COMMENT '公司名称',
    `position` VARCHAR(100) NOT NULL COMMENT '职位名称',
    `department` VARCHAR(100) COMMENT '部门',
    `start_date` DATE NOT NULL COMMENT '开始时间',
    `end_date` DATE COMMENT '结束时间',
    `is_current` TINYINT DEFAULT 0 COMMENT '是否在职：0-否，1-是',
    `salary` VARCHAR(50) COMMENT '薪资',
    `description` TEXT COMMENT '工作描述',
    `achievements` TEXT COMMENT '工作业绩',
    `sort_order` INT DEFAULT 0 COMMENT '排序序号',
    PRIMARY KEY (`id`),
    KEY `idx_resume_id` (`resume_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='工作经历表';
```

### 2.6 项目经历表（resume_project）

```sql
CREATE TABLE `resume_project` (
    `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `resume_id` BIGINT NOT NULL COMMENT '简历ID',
    `project_name` VARCHAR(100) NOT NULL COMMENT '项目名称',
    `role` VARCHAR(100) COMMENT '项目角色',
    `start_date` DATE NOT NULL COMMENT '开始时间',
    `end_date` DATE COMMENT '结束时间',
    `description` TEXT COMMENT '项目描述',
    `responsibility` TEXT COMMENT '职责描述',
    `technology_stack` VARCHAR(500) COMMENT '技术栈',
    `achievements` TEXT COMMENT '项目成果',
    `sort_order` INT DEFAULT 0 COMMENT '排序序号',
    PRIMARY KEY (`id`),
    KEY `idx_resume_id` (`resume_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='项目经历表';
```

### 2.7 技能表（resume_skill）

```sql
CREATE TABLE `resume_skill` (
    `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `resume_id` BIGINT NOT NULL COMMENT '简历ID',
    `skill_name` VARCHAR(100) NOT NULL COMMENT '技能名称',
    `skill_category` VARCHAR(50) COMMENT '技能分类',
    `proficiency_level` TINYINT COMMENT '熟练程度：1-了解，2-熟悉，3-精通',
    `used_years` INT COMMENT '使用年限',
    `sort_order` INT DEFAULT 0 COMMENT '排序序号',
    PRIMARY KEY (`id`),
    KEY `idx_resume_id` (`resume_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='技能表';
```

### 2.8 面试记录表（interview）

```sql
CREATE TABLE `interview` (
    `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '面试ID',
    `user_id` BIGINT NOT NULL COMMENT '用户ID',
    `position` VARCHAR(100) NOT NULL COMMENT '面试职位',
    `company` VARCHAR(100) COMMENT '公司名称',
    `interview_type` TINYINT DEFAULT 1 COMMENT '面试类型：1-电话面试，2-视频面试，3-现场面试，4-笔试',
    `interview_date` DATETIME COMMENT '面试时间',
    `location` VARCHAR(200) COMMENT '面试地点',
    `interviewer` VARCHAR(100) COMMENT '面试官姓名',
    `contact_phone` VARCHAR(20) COMMENT '联系电话',
    `status` TINYINT DEFAULT 0 COMMENT '状态：0-待面试，1-已完成，2-已取消，3-待定',
    `remark` TEXT COMMENT '备注',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`),
    KEY `idx_user_id` (`user_id`),
    KEY `idx_interview_date` (`interview_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='面试记录表';
```

### 2.9 面试题目表（interview_question）

```sql
CREATE TABLE `interview_question` (
    `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '题目ID',
    `interview_id` BIGINT NOT NULL COMMENT '面试ID',
    `question_type` TINYINT COMMENT '题目类型：1-技术题，2-行为题，3-场景题，4-其他',
    `question` TEXT NOT NULL COMMENT '题目内容',
    `answer` TEXT COMMENT '参考答案',
    `difficulty` TINYINT DEFAULT 2 COMMENT '难度：1-简单，2-中等，3-困难',
    `score` INT COMMENT '分值',
    `is_ai_generated` TINYINT DEFAULT 0 COMMENT '是否AI生成：0-否，1-是',
    `created_by` BIGINT COMMENT '创建人（AI或用户ID）',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (`id`),
    KEY `idx_interview_id` (`interview_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='面试题目表';
```

### 2.10 面试回顾表（interview_review）

```sql
CREATE TABLE `interview_review` (
    `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '回顾ID',
    `interview_id` BIGINT NOT NULL COMMENT '面试ID',
    `user_id` BIGINT NOT NULL COMMENT '用户ID',
    `self_evaluation` TEXT COMMENT '自我评价',
    `question_feedback` TEXT COMMENT '题目反馈',
    `performance_rating` TINYINT COMMENT '自我评分（1-5分）',
    `improvement_points` TEXT COMMENT '待改进点',
    `notes` TEXT COMMENT '其他记录',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_interview_id` (`interview_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='面试回顾表';
```

### 2.11 职位表（job）

```sql
CREATE TABLE `job` (
    `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '职位ID',
    `title` VARCHAR(100) NOT NULL COMMENT '职位名称',
    `company` VARCHAR(100) COMMENT '公司名称',
    `company_size` VARCHAR(50) COMMENT '公司规模',
    `industry` VARCHAR(50) COMMENT '所属行业',
    `location` VARCHAR(100) COMMENT '工作地点',
    `address` VARCHAR(200) COMMENT '详细地址',
    `salary_min` INT COMMENT '最低薪资（K/月）',
    `salary_max` INT COMMENT '最高薪资（K/月）',
    `salary_avg` DECIMAL(10,2) COMMENT '平均薪资',
    `experience_min` INT COMMENT '最低经验要求（年）',
    `experience_max` INT COMMENT '最高经验要求（年）',
    `education` TINYINT COMMENT '学历要求：1-大专，2-本科，3-硕士，4-博士',
    `employment_type` VARCHAR(20) COMMENT '全职/兼职/实习',
    `description` TEXT COMMENT '职位描述',
    `requirements` TEXT COMMENT '任职要求',
    `welfare` VARCHAR(500) COMMENT '福利待遇',
    `skills_required` VARCHAR(500) COMMENT '所需技能（JSON数组）',
    `source` VARCHAR(50) COMMENT '信息来源',
    `source_url` VARCHAR(500) COMMENT '原始链接',
    `publish_date` DATE COMMENT '发布日期',
    `deadline` DATE COMMENT '截止日期',
    `status` TINYINT DEFAULT 1 COMMENT '状态：0-下架，1-招聘中',
    `view_count` INT DEFAULT 0 COMMENT '浏览次数',
    `apply_count` INT DEFAULT 0 COMMENT '申请次数',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`),
    KEY `idx_title` (`title`(100)),
    KEY `idx_company` (`company`(50)),
    KEY `idx_location` (`location`(50))
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='职位表';
```

### 2.12 会员套餐表（member_package）

```sql
CREATE TABLE `member_package` (
    `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '套餐ID',
    `name` VARCHAR(50) NOT NULL COMMENT '套餐名称',
    `description` VARCHAR(500) COMMENT '套餐描述',
    `price` DECIMAL(10,2) NOT NULL COMMENT '价格',
    `duration_days` INT NOT NULL COMMENT '时长（天）',
    `points_gift` INT DEFAULT 0 COMMENT '赠送积分',
    `features` JSON COMMENT '功能权限',
    `sort_order` INT DEFAULT 0 COMMENT '排序序号',
    `status` TINYINT DEFAULT 1 COMMENT '状态：0-停售，1-在售',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='会员套餐表';
```

### 2.13 会员订阅表（member_subscription）

```sql
CREATE TABLE `member_subscription` (
    `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '订阅ID',
    `user_id` BIGINT NOT NULL COMMENT '用户ID',
    `package_id` BIGINT NOT NULL COMMENT '套餐ID',
    `start_time` DATETIME NOT NULL COMMENT '开始时间',
    `end_time` DATETIME NOT NULL COMMENT '结束时间',
    `status` TINYINT DEFAULT 1 COMMENT '状态：0-已过期，1-生效中',
    `auto_renew` TINYINT DEFAULT 0 COMMENT '是否自动续费：0-否，1-是',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`),
    KEY `idx_user_id` (`user_id`),
    KEY `idx_end_time` (`end_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='会员订阅表';
```

### 2.14 支付订单表（payment_order）

```sql
CREATE TABLE `payment_order` (
    `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '订单ID',
    `order_no` VARCHAR(64) NOT NULL COMMENT '订单号',
    `user_id` BIGINT NOT NULL COMMENT '用户ID',
    `product_type` TINYINT NOT NULL COMMENT '产品类型：1-会员套餐，2-积分充值',
    `product_id` BIGINT NOT NULL COMMENT '产品ID',
    `amount` DECIMAL(10,2) NOT NULL COMMENT '支付金额',
    `pay_method` VARCHAR(20) COMMENT '支付方式：alipay, wechat',
    `pay_time` DATETIME COMMENT '支付时间',
    `transaction_no` VARCHAR(100) COMMENT '第三方交易号',
    `status` TINYINT DEFAULT 0 COMMENT '状态：0-待支付，1-已支付，2-已取消，3-已退款',
    `remark` VARCHAR(500) COMMENT '备注',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_order_no` (`order_no`),
    KEY `idx_user_id` (`user_id`),
    KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='支付订单表';
```

### 2.15 积分交易表（points_transaction）

```sql
CREATE TABLE `points_transaction` (
    `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '交易ID',
    `user_id` BIGINT NOT NULL COMMENT '用户ID',
    `transaction_type` TINYINT NOT NULL COMMENT '交易类型：1-收入，2-支出',
    `points` INT NOT NULL COMMENT '积分数量',
    `balance` INT NOT NULL COMMENT '交易后余额',
    `source` VARCHAR(50) COMMENT '来源：signin, interview, member, payment, consume',
    `source_id` BIGINT COMMENT '来源关联ID',
    `description` VARCHAR(255) COMMENT '描述',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (`id`),
    KEY `idx_user_id` (`user_id`),
    KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='积分交易表';
```

### 2.16 用户反馈表（feedback）

```sql
CREATE TABLE `feedback` (
    `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '反馈ID',
    `user_id` BIGINT NOT NULL COMMENT '用户ID',
    `feedback_type` TINYINT COMMENT '反馈类型：1-Bug反馈，2-功能建议，3-其他',
    `title` VARCHAR(100) COMMENT '反馈标题',
    `content` TEXT NOT NULL COMMENT '反馈内容',
    `contact_info` VARCHAR(100) COMMENT '联系方式',
    `images` JSON COMMENT '截图URL数组',
    `status` TINYINT DEFAULT 0 COMMENT '状态：0-待处理，1-处理中，2-已处理',
    `reply_content` TEXT COMMENT '回复内容',
    `reply_time` DATETIME COMMENT '回复时间',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`),
    KEY `idx_user_id` (`user_id`),
    KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户反馈表';
```

### 2.17 文件上传记录表（file_upload）

```sql
CREATE TABLE `file_upload` (
    `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '记录ID',
    `user_id` BIGINT COMMENT '用户ID',
    `file_name` VARCHAR(200) NOT NULL COMMENT '文件名',
    `file_path` VARCHAR(500) NOT NULL COMMENT '文件路径',
    `file_size` BIGINT NOT NULL COMMENT '文件大小（字节）',
    `file_type` VARCHAR(50) COMMENT '文件类型',
    `upload_type` VARCHAR(50) COMMENT '上传类型：resume, avatar, other',
    `status` TINYINT DEFAULT 1 COMMENT '状态：0-失败，1-成功',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (`id`),
    KEY `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='文件上传记录表';
```

### 2.18 操作日志表（operation_log）

```sql
CREATE TABLE `operation_log` (
    `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '日志ID',
    `user_id` BIGINT COMMENT '用户ID',
    `username` VARCHAR(50) COMMENT '用户名',
    `operation` VARCHAR(50) COMMENT '操作类型',
    `method` VARCHAR(200) COMMENT '请求方法',
    `params` TEXT COMMENT '请求参数',
    `ip` VARCHAR(50) COMMENT 'IP地址',
    `user_agent` VARCHAR(500) COMMENT 'User-Agent',
    `status` TINYINT COMMENT '状态：0-失败，1-成功',
    `error_msg` TEXT COMMENT '错误信息',
    `duration` INT COMMENT '耗时（毫秒）',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (`id`),
    KEY `idx_user_id` (`user_id`),
    KEY `idx_operation` (`operation`),
    KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='操作日志表';
```

## 三、索引优化建议

### 3.1 常用查询索引

| 表名 | 索引字段 | 查询场景 |
|------|----------|----------|
| user | phone, email | 登录查询 |
| resume | user_id | 用户简历列表 |
| interview | user_id, interview_date | 面试日历 |
| job | title, location, industry | 职位搜索 |
| payment_order | order_no, status | 订单查询 |
| points_transaction | user_id, create_time | 积分记录 |

### 3.2 分区建议

- **operation_log**: 按 create_time 月分区
- **payment_order**: 按 create_time 月分区
- **points_transaction**: 按 user_id + create_time 分区

## 四、数据初始化

### 4.1 初始化会员套餐数据

```sql
INSERT INTO `member_package` (`name`, `description`, `price`, `duration_days`, `points_gift`, `features`, `sort_order`, `status`) VALUES
('月度会员', '解锁全部功能，有效期30天', 29.90, 30, 100, '{"all_features": true}', 1, 1),
('季度会员', '解锁全部功能，有效期90天', 79.90, 90, 300, '{"all_features": true}', 2, 1),
('年度会员', '解锁全部功能，有效期365天', 299.90, 365, 1000, '{"all_features": true}', 3, 1);
```

### 4.2 初始化系统配置

```sql
INSERT INTO `system_config` (`config_key`, `config_value`, `description`) VALUES
('site_name', '职引未来', '网站名称'),
('site_logo', '/logo.png', '网站Logo'),
('default_avatar', '/default-avatar.png', '默认头像'),
('ai_model', 'qwen3.5-plus', 'AI模型名称'),
('points_daily_signin', '10', '每日签到积分'),
('points_interview_complete', '20', '完成面试记录积分');
```